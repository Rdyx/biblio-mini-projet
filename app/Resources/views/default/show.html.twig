{% extends 'base.html.twig' %}

{% block body %}
    <h1>Livre</h1>

    <table>
        <tbody>
        <tr>
            <th>Id</th>
            <td>{{ livre.id }}</td>
        </tr>
        <tr>
            <th>Name</th>
            <td>{{ livre.livTitre }}</td>
        </tr>
        <tr>
            <th>Price</th>
            <td>{{ livre.livAuteur }}</td>
        </tr>
        <tr>
            <th>Description</th>
            <td>{{ livre.livDescription }}</td>
        </tr>
        <tr>
            <th>Description</th>
            <td>{{ livre.livDateParution }}</td>
        </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('livre_index') }}">Back to the list</a>
        </li>
        {#<li>#}
            {#<a href="{{ path('livre_edit', { 'id': product.id }) }}">Edit</a>#}
        {#</li>#}
        {#<li>#}
            {{ form_start(delete_form) }}
            <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        {#</li>#}
    </ul>
{% endblock %}
